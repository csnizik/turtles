//build project type.

export interface IProject {
  agreementNumber: string;
  awardeeName: string;
  title: string;
  funds: number;
  state: string;
  year: number;
  resource: string;
  description: string;
  deliverables: string;
}
